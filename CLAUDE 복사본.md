# CLAUDE.md

이 파일은 이 프로젝트에서 작업할 때 Claude Code가 참고하는 프로젝트 컨텍스트입니다.

---

## 프로젝트 개요

**AR 애착인형 밥친구 앱** (해커톤 프로젝트)

> 한 줄 요약: 카메라 AR 기술로 아이의 '실물 인형'에 생명을 불어넣어 화면 속에 2D 이미지로 띄워줌.

### 페르소나
- 아이에게 미디어를 쥐어주고 죄책감을 느끼는 부모
- 아이가 밥을 오래 먹어 답답한 부모
- 미디어를 보여줬다는 죄책감 대신, **아이의 실제 애착 인형을 친구로 만들어** 긍정적 정서 교감을 선물하고 싶은 부모

### 핵심 기능
1. **2D 인형 교감** — 등록된 인형이 통통 튀기 / 종이접기처럼 반 접혔다 펴지기 / 오뚝이처럼 좌우 움직임 + 인사·질문·리액션 → 토킹톰 방식의 의사소통 (배경은 서비스 제공 이미지)
2. **AR 밥상 친구** — 밥 먹는 동안 폰을 아이 앞에 거치 → 2D 인형이 밥상 위에서 살아 움직임 (음식 근처 어슬렁, 옆에서 음악, 같이 밥 먹기, 자연스러운 식사시간 제한)

### 어색함을 줄이는 기술 포인트
- 촬영 이미지를 그대로 쓰지 않고 **유아틱한 그림체로 변환/애니메이션화** → AR 이질감 감소
- 2D 이미지가 항상 **AR 카메라를 정면으로 바라보도록 회전값 실시간 제어** (빌보드 기법) → 폰을 움직여도 납작한 옆면 노출 안 됨
- **동적 그림자 추가** → 실제로 서 있는 듯한 착시

---

## 화면 구조 (Figma 플로우차트 기준)

화면 ID는 Figma 플로우차트 네이밍을 그대로 사용합니다.

| 화면 ID | 설명 | 주요 요소 |
|---|---|---|
| `MainEmpty` | 온보딩 / 빈 메인 | "아직 친구가 없습니다" + `친구 만들기` 버튼, 하단 탭 |
| `EmptyToast` | 제지 안내 | "먼저 아이의 애착 인형을 등록해 주세요!" |
| `DollScan` | 인형 촬영(스캔) | 카메라 가이드 프레임, 셔터 버튼, 촬영 TIP |
| `DollNameInput` | 이름 입력 | "이름을 지어주세요!" 입력창 + `저장` |
| `DollLoading` | 생성 중 모달 | "친구를 데려오는 중이에요." |
| `MainHome` | 메인 홈 (친구 있음) | "우와! 00가 세상에 찾아왔어요!", 배경 이미지 (1)~(4), `+ 새 친구 등록하기` |
| `MealAR` | AR 밥친구 모드 | "안녕! 오늘 나랑 같이 밥 먹을까?" / "밥 먹는 동안 노래를 불러줄게!" |
| `TalkNode` | 대화/놀기 모드 | 캐릭터 모션 + 대화 |
| `MealReport` | 식사 완료 리포트 | "오늘 00이가 [인형]이와 함께 18분 만에 밥을 다 먹었어요!" |

### 플로우

```
MainEmpty ──(밥먹기/놀기 클릭)──> EmptyToast ──> DollScan
    │
    └─(친구 만들기)─> DollScan ─> DollNameInput ─> DollLoading ─> MainHome
                                                                    │
                                        ┌───────────────────────────┤
                                        │                           │
                                   (밥 먹기 탭)                 (놀기 탭)
                                        ↓                           ↓
                                     MealAR                     TalkNode
                                        ↓ (식사 완료)
                                    MealReport ──> MainHome
```

하단 탭은 전 화면 공통: `🍴 밥 먹기` / `😊 놀기`

---

## 팀 구성 및 역할

| 파트 | 담당 | 결정 사항 |
|---|---|---|
| 백엔드/앱 | 팀원 | **안드로이드 앱**, Android Studio 사용. 기본 틀 잡고 Git 푸시 예정 |
| **AI** | **나 (이병헌)** | 이미지 변환 + 음성(STT/TTS) + 대화 생성 |

### AI 파트 담당 과제

| # | 과제 | 상태 |
|---|---|---|
| 1 | 사진 → 그림 변환에 최적화된 AI 조사·선정 | ✅ **완료** (미검증 리스크 3건 있음, 아래 참조) |
| 2 | 음성 생성 AI (TTS) 조사 — ElevenLabs 등 | ✅ **완료** (과제 4 결과로 Live API 채택 → Gemini voice 확정) |
| 3 | TTS AI가 LLM처럼 질문 생성 기능이 있는지 확인 | ✅ **완료** (된다. 그러나 쓰면 안 된다 — 아래 참조) |
| 4 | STT → LLM → TTS 왕복 지연 시간 측정 | ✅ **완료** (경로 A/B/C 실측, C 압승) |

---

## 기술 결정 사항

### 이미지 변환 파이프라인 (과제 1)

```
촬영 → [1] rembg 사전 누끼(손·배경 제거) → 흰 배경 합성
     → [2] Gemini 스타일 변환 + 순백 배경 지시
     → [3] 코드 누끼(44ms) → Android AR 빌보드
```

> 📌 **2026-08-04 개정.** rembg 2회 → **1회**로 줄였다. 뒤쪽(변환 결과 누끼)만 코드로 대체된다.
> **앞쪽 사전 누끼는 반드시 남겨야 한다** — 생략하면 캐릭터 일관성이 무너진다(아래 실측).

- **스타일 변환 모델: `gemini-3.1-flash-image` 확정** (실측 비교 완료)

  | | Flash | Pro |
  |---|---|---|
  | 색 충실도 | 🟢 원본 올리브 그린 유지 | 🔴 파스텔로 밝아짐 |
  | 없는 특징 추가 | 🟢 없음 | 🔴 코를 그려 넣음 |
  | 아웃라인 | 🟢 두꺼움(AR 유리) | 🟡 얇음 |
  | 속도 / 비용 | 🟢 12.4초 / 65원 | 🔴 20.9초 / 200원 |

  → **Pro는 "더 잘 그리려고" 해석을 더 하고, 그게 정체성 훼손으로 이어진다.**
  이 과제엔 **덜 해석하는 모델**이 맞다. 비싼 모델을 쓰라고 권하지 말 것.

- **백업**: `gpt-image-2` — `background: transparent` 네이티브 지원 (미검증)

- **배경 제거: 🟢 Gemini에게 순백 배경을 시키고 코드로 자른다. rembg 불필요.**

  **① Gemini는 알파 채널을 만들지 못한다.** 투명 PNG를 명시적으로 요구해도 **3/3 전부 RGB**로 나온다.
  AI Studio에서 "배경 제거가 잘 되는 것처럼 보이는" 건 배경이 흰색이라 그렇게 보이는 것이고,
  **AR 빌보드에 필요한 알파는 없다.** 이걸 그대로 쓰면 인형 주변에 흰 사각형이 뜬다.

  **② 대신 순백 배경은 완벽하게 만든다.** 단, **스타일 변환과 동시에** 요구했을 때만이다.

  | 요구 방식 | 모서리 RGB | 판정 |
  |---|---|---|
  | 배경 제거만 (투명 요구) | 233.6 ± 20.2 | 🔴 회색빛, 불균일 |
  | 배경 제거만 (순백 요구) | 248/233/226 ± 32.3 | 🔴 색이 틀어짐 |
  | **스타일 변환 + 순백 동시** | 🟢 **254.7 ± 0.6** | 🟢 **4/4 재현** |

  → **"배경만 지워줘"는 오히려 지저분하다.** 원본을 편집하려 하기 때문이다.
  새로 그리게 하면서 배경을 지정하면 깨끗하게 나온다.

  **③ 순백이면 코드로 자르는 게 압도적으로 빠르다.**

  | | 시간 | 투명 비율 |
  |---|---|---|
  | rembg `birefnet-general` | 12~25초 | 68.4% |
  | **flood fill 코드 누끼** | 🟢 **44ms** | 68.1% |

  약 **300배 빠르고 결과는 사실상 동일**하다. 973MB 모델 파일도, ML Kit 이관 검토도 불필요해졌다.
  구현: 가장자리에 연결된 흰색만 `label()` 로 골라 제거 → `binary_fill_holes` → 경계 `sigma=0.6` 페더링.
  **단순 임계값(`>245` 전부 투명)은 쓰지 말 것** — 눈 하이라이트까지 뚫린다.

  **④ 🔴 원본 사진의 사전 누끼는 생략하면 안 된다 (반증됨).**

  배경 지시만으로 손·배경이 사라지길래 사전 누끼가 불필요해 보였다. **배경만 보면 실제로 성공한다**
  (원본 직접 입력도 모서리 254.7±0.7, 투명 67~68%). 그러나 **캐릭터가 매번 달라진다.**

  | 입력 | 배경 품질 | 캐릭터 일관성 (같은 프롬프트 3~4회) |
  |---|---|---|
  | 원본 사진 직접 | 🟢 254.7±0.7 | 🔴 **자세·표정·등껍질 무늬가 전부 다름.** 정면/누움/위에서 본 각도가 섞여 나옴 |
  | rembg 사전 누끼 후 | 🟢 254.7±0.6 | 🟢 **3/3 정면·웃는 얼굴·같은 무늬** |

  → 손과 배경이 남아 있으면 모델이 **형태를 매번 다르게 해석한다.**
  실루엣을 미리 정리해서 주면 해석의 여지가 사라져 일관성이 회복된다.

  **이건 R1(스프라이트 6장 간 일관성)과 직결된다.** 입 3장이 초당 20~30회 교체되는데 자세가
  흔들리면 립싱크가 통째로 무너진다. **사전 누끼는 일관성 장치이지 배경 제거 수단이 아니다.**

  ⚠️ 교훈: 배경이 깨끗해진 것만 보고 성공으로 판정할 뻔했다. **누끼 품질과 캐릭터 일관성은 별개 축이다.**

  ⚠️ **흰색·크림색 인형은 이 방법이 위험하다.** 캐릭터와 배경이 같은 색이 되어 몸통이 잘려나간다.
  흰 인형에서는 배경색을 마젠타(#FF00FF) 같은 보색으로 지정하는 대안을 검토할 것. (미검증)

**중요 제약**
- ❌ **텍스트→이미지 생성 모델 사용 금지** — "아이의 그 인형"이어야 하므로 반드시 image-to-image 편집
- ❌ **인물 얼굴 전용 카툰화 툴 사용 금지** — 봉제인형은 얼굴 랜드마크가 없어 결과가 무너짐
- ⚠️ 스타일 변환 전에 **먼저 누끼**를 떠야 함 (배경의 소파·식탁까지 그려버림)

### 음성 생성 (과제 2) — 진행 중, 실측 기록

**Gemini TTS 실측** (한국어, `안녕! 오늘 나랑 같이 밥 먹을까?` 등 기획서 대사 3종)

| 모델 | 비스트리밍 총 지연 | 스트리밍 TTFB | 출력 단가 |
|---|---|---|---|
| `gemini-3.1-flash-tts-preview` | 3.2~5.0초 | **1.1~1.6초** (3회 반복) | $20/1M |
| `gemini-2.5-flash-preview-tts` | 4.1~4.8초 | ❌ 미지원 | $10/1M |
| `gemini-2.5-pro-preview-tts` | 5.1~6.6초 | ❌ 미지원 | $20/1M |

- **오디오 32 토큰/초** (voice 무관하게 일정) → 3.1 flash 기준 **약 0.9원/초**, 2.5 flash는 0.45원/초
- **2.5 pro 탈락 확정.** 가장 느린데(5~6.6초) 단가는 3.1 flash와 같다. 이미지 모델과 동일한 결론.
- **스트리밍은 선택이 아니라 필수.** 비스트리밍 3~5초면 아이가 말 건 뒤 인형이 죽은 것처럼 보인다.
  스트리밍 지원은 `gemini-3.1-flash-tts-preview` 부터.
- 한국어 8개 voice 전부 정상 생성, 실패 0건. voice 후보: `Leda`(youthful) `Puck` `Zephyr` `Achernar` `Laomedeia` `Sulafat` `Aoede` `Callirrhoe`
- Gemini TTS는 **자연어로 연기 지시가 가능**하다 (`STYLE_DIRECTION`). ElevenLabs Flash는 불가(보이스 자체로만 결정).

**⚠️ 이미지 모델과 달리 TTS는 계정에서 바로 호출됐다.** 429 없음.

**립싱크 실현성 — ✅ 검증 완료**

WAV의 RMS 엔벨로프를 30fps로 뽑으면 `closed 59 / half 14 / open 69` 로 고르게 분포한다.
→ **Android가 오디오 재생 중 실시간 RMS로 같은 계산을 하면 된다. AI 파트 추가 작업 없음.**
임계값은 정규화 RMS 기준 `<0.08 closed / <0.30 half / else open`.

**ElevenLabs 실측 (2026-08-04 완료)**

| 모델 | TTFB | 총 |
|---|---|---|
| `eleven_flash_v2_5` | **0.25~0.52초** | 0.33~0.61초 |
| `eleven_multilingual_v2` | 1.0초 | 1.0초 |
| `eleven_v3` | 1.1초 | 2.3~2.7초 |

- **Flash v2.5가 Gemini 스트리밍(1.3초)보다 3~5배 빠르다.** 공식 75ms 주장에는 못 미치나 한국에서 호출한 값.
- 계정 프리셋 voice 21개가 **전부 `language=en`(영어 화자)**. 그런데도 한국어 발음은 정상이었다(아래 참조).
- 기본 목록 앞쪽은 중년 남성 내레이터에 몰려 있어 그냥 상위 N개를 뽑으면 못 쓸 목소리만 테스트하게 된다.
  → `eleven_doll_score()` 로 young/female/characters_animation 을 가점해 정렬 후 스크리닝하도록 처리함.
- 무료 플랜: 월 10,000 크레딧 (Flash는 문자당 0.5 → 20,000자). **상업 이용 불가**(데모는 무관).

**한국어 발음 명료도 — STT 역검증으로 확인**

TTS 결과를 `gemini-3.5-flash` 로 다시 전사해 원문과 대조:

| | 일치율 |
|---|---|
| Gemini Leda | 100% |
| Gemini 나머지 / ElevenLabs 전부 | 94.7% (차이는 `!` vs `,` 문장부호뿐) |

→ **발음 명료도로는 두 엔진을 가를 수 없다.** 영어 화자 voice도 한국어 단어를 정확히 발음한다.
단, STT는 억양이 어색해도 알아듣기 때문에 **"원어민스러운 억양"까지 검증한 것은 아니다.**

**🔴 LLM 청취 평가는 실패했다 (기록용 — 다시 시도하지 말 것)**

Gemini에 오디오를 주고 인형 목소리 적합도를 채점시켜 봤으나 **순위를 재현하지 못한다.**

| | 1차 | 2차(3회 평균) |
|---|---|---|
| Leda | 20점 | 15.3점 |
| Callirrhoe | 14점 | **19.0점** |

같은 파일을 4회 평가하면 총점이 최대 6점 흔들린다. 상위 후보들은 평균 17.5~18.5로 구별 불가.
→ **명백히 나쁜 후보를 거르는 1차 필터로만 쓰고, 최종 선택은 반드시 사람이 귀로 한다.**
(`--judge` 로 남겨두되 편차 폭을 항상 같이 출력하게 함)

**목소리 커스터마이징 구조 (자주 오해되는 부분)**

레버는 **두 개뿐**이고 성격이 다르다.

| | 무엇 | 바꿀 수 있나 |
|---|---|---|
| `voice_name` | 성대. Google 제공 **30개 중 택1** | 선택만 가능. 음색·음높이 조절 불가, **커스텀 보이스 학습 불가** |
| 프롬프트(`STYLE_DIRECTION`) | 연기 지시. 톤·속도·감정 | 🟢 **자유롭게 작성 — 여기가 진짜 커스터마이징** |

Gemini TTS는 "이 문장을 읽어라"가 아니라 **"너는 이런 캐릭터다 + 이렇게 말해라 + 대사는 이거다"** 를
하나의 프롬프트로 받는다 (`prompt = f"{style}\n\nNow say this line in Korean:\n{text}"`).

**실측 증거** — 같은 Leda, 같은 대사 `안녕! 오늘 나랑 같이 밥 먹을까?`:

| 연기 지시 | 오디오 길이 |
|---|---|
| 지시 없음 | 3.3초 |
| `friend` | 3.7초 |
| `whisper` | 3.6초 |
| `sleepy` ("천천히 늘여서") | **7.1초** |

→ 프롬프트가 실제로 먹힌다. **앱에서는 voice_name을 인형마다 고정하고, 상황별로 지시문만 교체한다.**
`STYLE_VARIANTS` 에 기획 상황별 6종 보관: `none`/`friend`/`excited`(MealReport)/`sleepy`(표정 스프라이트와 짝)/`singsong`("노래 불러줄게")/`whisper`

- 인형 여러 개 등록 시 → **인형마다 다른 voice_name 배정**으로 각자 다른 목소리를 줄 수 있다. 30개면 충분.
- ⚠️ voice 특성 라벨(`Youthful`, `Excitable` 등)은 **공식 문서의 설명일 뿐 API 입력값이 아니다.**
  코드의 `note` 필드는 화면 출력·기록용이며 API로 전송되지 않는다.
- ⚠️ 간헐적으로 응답 `parts` 가 비어서 온다(6회 중 1회). **재시도 로직 필수.**

**TTS 단독 비교 (참고용 — 최종 결론은 과제 4 참조)**

| | Gemini 3.1 Flash TTS | ElevenLabs Flash v2.5 |
|---|---|---|
| TTFB | 1.1~1.6초 | 🟢 **0.25~0.52초** |
| 자연어 연기 지시 | 🟢 가능 (위 실측 참조) | 🔴 **불가** — 지시문 입력 자체가 없음. `stability` 등 숫자 슬라이더만 |
| 커스텀 보이스 | 🔴 불가 (30개 중 배정) | 🟢 voice cloning (유료 플랜) |
| 한국어 명료도 | 🟢 | 🟢 |
| 추가 결제 | 불필요 | 무료 크레딧 소진 후 필요 |

→ **TTS만 놓고 보면 ElevenLabs가 빠르다. 그러나 과제 4에서 이 비교 자체가 무의미해졌다.**
Live API는 STT·LLM·TTS를 하나로 합쳐서 0.85초를 내는데, ElevenLabs로 3단을 조립하면 2.71초다.
**부품이 빠른 것과 완성품이 빠른 것은 다르다.**

### 과제 3 — TTS 단독 대사 생성 (결론: 되지만 쓰지 말 것)

`gemini-3.1-flash-tts-preview` 에 "대답을 지어내서 말해줘"를 시키면 **실제로 지어낸다.**
하지만 **지시문을 무슨 언어로 쓰느냐에 따라 동작이 갈린다.**

| 지시문 언어 | 결과 |
|---|---|
| **영어** | 🟢 생성 4/4 성공 |
| **한국어** | 🔴 지시문을 그대로 낭독하거나 `400 INVALID_ARGUMENT` (0/4) |

그런데 영어 지시로 성공해도 **한국어 출력 품질이 무너진다.**

> `킨더가튼에서 그림 그렸어?` ← 영어 프롬프트의 `kindergarten`을 그대로 음차
> `킨더가든에서 그림 그렸대 어떤 그림 그렸어` ← 문장 붕괴

- 지연도 5.4~7.6초로 느리다 (LLM 분리 시 2.1초).
- 리액션 없이 질문만 나온다. 페르소나 규칙(20자 이내 등)이 안 지켜진다.
- ⚠️ **`system_instruction` 은 TTS 모델에서 미지원** → `Developer instruction is not enabled for this model` (400)

**ElevenLabs는 아예 불가능하다.** 같은 지시문을 넣으니 프롬프트 전체를 **43.6초 동안 낭독**했다.
순수 합성기이므로 넣은 글자를 읽는 것 외에 아무것도 하지 않는다.

→ **대사 생성은 반드시 LLM에 맡기고 TTS는 읽기만 시킨다.** 합치려 하지 말 것.

### 과제 4 — 왕복 지연 실측 → 🟢 **Live API(경로 C) 채택**

측정 기준: **아이가 말을 끝낸 순간 → 인형의 첫 소리** (TTFB). 총 소요가 아니다.
아이 발화 3종(`그림 그렸어` / `안 먹을래` / `이름이 뭐야`)으로 측정.

| 경로 | 구성 | 평균 TTFB |
|---|---|---|
| **C** | `gemini-3.1-flash-live-preview` 음성↔음성 | 🟢 **0.85초** (0.77~0.98) |
| A | 오디오→LLM→**ElevenLabs** | 2.71~2.77초 |
| B | 오디오→STT→LLM→**ElevenLabs** | 3.55초 |
| A | 오디오→LLM→**Gemini TTS** | 4.15~4.32초 |
| B | 오디오→STT→LLM→**Gemini TTS** | 4.97~5.38초 |

**Live가 3배 빠르다.** 아이는 1초 넘는 침묵을 "인형이 죽었다"로 받아들인다. 이 격차는 결정적이다.

**🔴 최대의 레버는 모델이 아니라 `thinking` 이었다**

첫 측정에서 경로 A가 10.5초였다. 원인은 **Gemini 3.x의 기본 thinking 활성화**.

| | LLM 단계 |
|---|---|
| thinking 기본값 | 7.8~10.9초 |
| `thinking_budget=0` | 🟢 **2.1초** |

인형 대사는 20자짜리 리액션이라 추론이 필요 없다. **대화 경로에서는 항상 thinking을 끈다.**
(`ThinkingConfig(thinking_budget=0)`. 단 `gemini-3.5-flash-lite`는 이 옵션에서 400이 난다.)

**부수 결론**
- **STT를 분리하면 손해다.** 경로 A(오디오를 LLM에 직접)가 B보다 0.8~1.1초 빠르다. STT 단계가 통째로 사라진다.
- B 경로는 품질도 나쁘다. 전사를 거치며 인형이 아이 말을 자기 말로 착각한다
  (`응 나 알록달록 무지개 그렸어` ← 아이가 한 말인데 인형이 자기가 그렸다고 답함).

**Live API 구현 시 주의 (실측으로 걸린 것)**

- 🔴 **`audio_stream_end=True` 는 동작하지 않는다.** 응답이 오지 않고 멎는다(35초 타임아웃).
  → `AutomaticActivityDetection(disabled=True)` + **`ActivityStart`/`ActivityEnd` 수동 신호**를 쓸 것.
  (침묵 1초를 꼬리에 붙이는 자동 VAD도 되지만 응답 완료가 18.7초로 느려진다)
- 입력은 **16kHz PCM**, 출력은 24kHz. 100ms(3200바이트) 청크로 보낸다.
- `system_instruction` 을 빼면 페르소나가 통째로 무시된다
  (→ `저는 유치원에 다니지 않아서 그림을 그리지는 않았어요` 같은 존댓말 어시스턴트 톤이 나옴).
  반드시 `types.Content(parts=[...])` 형태로 넘길 것.
- `output_audio_transcription` 을 켜면 인형이 뭐라고 말했는지 텍스트로 같이 받는다. **립싱크·로깅에 필요.**

**⚠️ Live 채택의 대가**

| 잃는 것 | 내용 |
|---|---|
| ElevenLabs | 목소리가 **Gemini 30개로 고정**. voice cloning 불가 |
| 응답 길이 제어 | Live는 수다스럽다. 발화가 7.2~7.5초까지 늘어짐(`20자 이내` 규칙 무시). **프롬프트 튜닝 필요** |
| 구현 단순함 | WebSocket 상시 연결 → Android 구현·배터리 부담이 REST보다 큼 |

→ 그럼에도 **0.85초 vs 2.71초** 앞에서는 다른 항목이 의미가 없다. Live로 간다.
립싱크는 Live도 오디오 스트림이 그대로 오므로 **기존 RMS 방식을 변경 없이 쓴다.**

### 비용 / 결제 (실측 확인됨)

- **Gemini API 유료 전환은 최소 $10 선불 충전이 필수**다. "종량제라 쓴 만큼만 낸다"는 잘못된 정보 — 그렇게 안내하지 말 것.
  - 약 16,000원 (VAT 10% 포함) / 크레딧 **12개월 후 만료** / **환불 불가**
  - Google Cloud 결제 계정 연결 필요
- 무료 티어에서 이미지 모델은 **할당량이 0**이다. 호출 시 `429 RESOURCE_EXHAUSTED (free_tier_requests, limit: 0)` 발생.
  - 단, `models.list()` 같은 조회는 무료로 되므로 **모델 목록이 보인다고 결제된 게 아니다.**
- `Google AI Plus` 구독(월 7,500원)은 소비자용 Gemini 앱 요금제로, **API 할당량과 무관**하다.

### 배경 제거 성능 (실측, M-series Mac CPU) — ⚠️ 이 경로는 폐기됨

> 아래는 rembg를 쓰던 시절의 기록이다. **현재 파이프라인은 코드 누끼(44ms)를 쓴다.**
> 순백 배경을 못 만드는 상황(흰 인형 등)의 백업으로만 참고할 것.

| 모델 | 세션 로딩 | 1536px 추론 |
|---|---|---|
| `birefnet-general` | 4초 | **25~54초** |
| `birefnet-general-lite` | 33초 | 17초 |

- CPU onnxruntime은 **서버 실시간 처리에 부적합**. 품질은 매우 우수(경계 깔끔).
- 모델 파일이 큼: `birefnet-general` 973MB, `lite` 224MB (`~/.u2net/` 에 캐시)

### 캐릭터 애니메이션 방식 (중요 - 자주 오해되는 부분)

**AI가 만드는 건 정지 PNG 몇 장뿐이다. 영상이 아니다.**
아이가 무슨 말을 할지 모르므로 대사에 맞는 영상을 미리 만드는 건 불가능하다.

```
AI가 미리 만드는 것    →  부품 (정지 PNG 5~6장)
Android가 실시간 하는 것 →  연출 (어느 부품을 언제, 어떻게 변형해 보여줄지)
```

| 원하는 동작 | 추가 PNG 필요? | 구현 |
|---|---|---|
| 통통 튀기 | ❌ 0장 | Y축 이동 + 스쿼시/스트레치 |
| 오똑이 좌우 흔들기 | ❌ 0장 | Z축 회전 ±15° |
| 반 접혔다 펴지기 | ❌ 0장 | Y축 스케일 0.7↔1.0 |
| 숨 쉬는 느낌(idle) | ❌ 0장 | 매 프레임 ±3px 흔들기 |
| **립싱크** | ✅ 입 모양 3장 | TTS 오디오 음량에 맞춰 초당 20~30회 교체 |
| 표정 변화 | ✅ 표정 2장 | 상태에 따라 교체 |
| 한 바퀴 돌기 | ⚠️ 뒷모습 1장 | 2D라 뒷면이 없음. 없으면 Z축 제자리 스핀으로 대체 |

**필요한 스프라이트 세트 (등록 시 1회 생성, 약 1MB)**
```
mouth_closed / mouth_half / mouth_open   ← 립싱크
happy / sleepy                            ← 표정
(back)                                    ← 선택
```

- **실시간 대화 중 AI 이미지 생성은 절대 불가.** 1장에 12.4초 걸린다(실측).
  → 반드시 **등록 시점 사전 생성(pre-generation)** 방식. `DollLoading` 모달이 이 시간을 커버한다.
- 6장 × 12.4초 ≈ 75초, 약 390원. 병렬 호출하면 20초 안쪽 가능.
- 향후 확장: 머리·지느러미를 부위별로 잘라 관절 회전(Live2D 방식)하면 생동감이 크게 올라감. 리깅에 하루 이상 필요하므로 해커톤에서는 후순위.
- ❌ AI 영상 생성(Veo 등)은 임의 대사 립싱크가 불가능하므로 이 서비스에 부적합.

### 아키텍처 제약

> 🔐 **API 키를 안드로이드 앱에 직접 넣지 말 것.** APK 디컴파일로 유출됨.

AI 파트 산출물은 **중계 서버** 형태로 제공한다.

```
Android ──(사진 업로드)──> AI 서버(FastAPI) ──> Gemini / TTS / STT API
        <──(투명 PNG, 음성)──                <──
```

앱 파트와의 인터페이스 계약:
- `POST /doll/stylize` : `multipart/form-data (image)` → `{ "sprites": ["url", ...] }`
- `WS /doll/talk` : 대화. Android ↔ 서버 ↔ Gemini Live API 를 **WebSocket으로 중계**한다.
  - 업: 16kHz PCM 청크(100ms) + `activity_start` / `activity_end`
  - 다운: 24kHz PCM 청크 + 발화 전사 텍스트(립싱크·로깅용)
  - ⚠️ 과제 4에서 Live API를 채택했으므로 **단순 REST 중계로는 안 된다.** 앱 파트에 이 점을 공유할 것.

### 지연 시간 관점

- **인형 등록(과제 1)**: `DollLoading` 모달이 기획에 이미 있음 → 5~15초 허용됨. **품질 최우선**.
- **대화(과제 2·4)**: 실시간 응답 필요 → **지연 최소화 최우선**. 스트리밍 TTS 필수 검토.

---

## 참고 자료

- Figma 기획 파일: `https://www.figma.com/design/uYCh5dOPgoUMVFQI4UfaX8/이거저거?node-id=0-1`
  - ⚠️ 현재 계정(`0417lbh@gmail.com`)이 **View 좌석**이라 Figma MCP로 읽을 수 없음. Editor 권한 필요.
  - 대안: 캔버스 스크린샷을 직접 첨부해서 전달

---

## 작업 규칙

- 모든 응답과 설명은 **한국어**로 작성한다. (기술 용어·코드 식별자는 원문 유지)
- 화면을 지칭할 때는 위 표의 **화면 ID**를 사용한다. (예: `MealAR`)
- AI 파트 과제 1~4는 **순서대로** 진행한다. 사용자가 명시적으로 요청하지 않으면 다음 과제로 넘어가지 않는다.
- 조사 과제는 "웹 검색 요약"으로 끝내지 말고, **실제로 돌려볼 수 있는 검증 스크립트**까지 만들어 제공한다.

### 📌 과제 진행 상태 자동 갱신 (필수)

과제가 진행되면 **사용자가 시키지 않아도 이 CLAUDE.md 파일을 직접 수정**해서 상태를 반영한다.

**갱신 시점**
- 과제를 시작할 때 → `⬜ 대기` → `🔄 진행 중`
- 과제가 끝났을 때 → `🔄 진행 중` → `✅ 완료`
- 기술 선택이 확정됐을 때 → `기술 결정 사항` 섹션에 근거와 함께 기록

**"완료"의 기준** — 아래를 모두 만족해야 `✅ 완료`로 바꾼다. 조사만 하고 완료 처리하지 말 것.
1. 후보 비교와 **최종 선정**이 끝났다
2. 실제로 **동작을 검증**했다 (스크립트 실행 또는 사용자 확인)
3. 선정 근거와 수치(비용·지연 등)가 `기술 결정 사항`에 기록됐다

**갱신 방법**
1. `AI 파트 담당 과제` 표의 상태 컬럼 수정
2. 확정된 내용을 `기술 결정 사항` 섹션에 추가
3. 아래 `작업 로그`에 한 줄 추가
4. 사용자에게 **"CLAUDE.md 갱신함: (무엇을)"** 이라고 한 줄로 보고

---

## ⚠️ 미검증 리스크 (과제 1에서 넘어온 것)

과제 1은 모델·파이프라인·프롬프트까지 확정했지만 아래 3건은 **확인하지 못한 채 종료**했다.
시연 전에 반드시 확인할 것. 특히 R1은 실패하면 립싱크 자체가 성립하지 않는다.

### 🔴 R1. 스프라이트 간 캐릭터 일관성 — 최우선

입 3장이 초당 20~30회 교체되는데, 그 사이 **등껍질 무늬나 색이 조금이라도 달라지면 캐릭터가 덜덜 떨린다.**
v2 실패 사례처럼 모델이 프레임마다 재설계하면 립싱크가 통째로 무너진다.

- 검증법: 같은 프롬프트로 입 3단계를 뽑아 알파 채널을 겹쳐보고 실루엣·색이 일치하는지 확인
- 비용: 약 325원
- 실패 시 대안: ① 1장만 생성 후 **입 부분만 잘라내 코드로 변형** ② 부위 분리 리깅

### 🟡 R2. 다른 인형에서도 되는가 (과적합)

**거북이 인형 딱 하나로만 프롬프트를 튜닝했다.** 거북이는 팔다리가 없고 형태가 특이한 편이라,
곰인형처럼 팔다리·주둥이가 뚜렷한 인형에서는 다르게 동작할 수 있다.

- 검증법: 곰인형 / 캐릭터 굿즈 등 2~3종으로 같은 프롬프트 실행
- 비용: 약 200원

### 🟡 R3. 뒷지느러미 소실

v3에서 뒤쪽 지느러미 2개가 생략됐다(v1에는 있었음). "지느러미 4개를 모두 유지하라"를
명시하지 않아서 생긴 문제로 추정. v4로 해결 가능해 보이나 **미검증**.

- 검증법: `PROMPT_V3` + `Keep ALL FOUR flippers visible, two front and two rear.` 추가 후 재생성
- 비용: 65원

### 🔴 R5. Live API 비용 미측정 — 시연 전 확인 필수

과제 4에서 **지연만 재고 비용은 재지 않았다.** Live API는 오디오 입출력을 상시 스트리밍하므로
TTS 단건 호출(0.9원/초)과 요금 구조가 다를 수 있다. 밥 한 끼(18분) 동안 계속 켜두는 서비스라
**단가가 높으면 기획 자체가 흔들린다.**

- 검증법: 한 세션의 `usage_metadata` 를 누적해 분당 토큰을 구한 뒤 단가표와 대조
- ⚠️ 무료 크레딧이 $10뿐이다. 시연 도중 소진되면 데모가 죽는다

### 🟡 R6. 실제 아이 목소리로 미검증

입력을 **TTS 합성음(4살 아이 연기 지시)** 으로 만들어 썼다. 실제 유아는 발음이 뭉개지고
문장이 끊기며 주변 소음(식기·TV)이 섞인다. Live API의 VAD가 이걸 발화 종료로 오인하면
인형이 말 중간에 끼어든다.

- 검증법: 실제 아이(또는 성인이 흉내낸) 녹음 + 식사 소음 배경으로 재측정

### 🟡 R7. Live 응답이 너무 길다

`20자 이내 한 문장` 규칙을 줬는데도 발화가 7.2~7.5초까지 늘어졌다.
(`우와! 뭐 그렸어? 초록이 보여줘! 초록이도 그려줘! 무슨 색으로 그릴까?`)
아이가 대답할 틈이 없고, 밥 먹는 시간을 오히려 잡아먹는다.

- 검증법: `DOLL_PERSONA` 를 조여 재측정. 안 되면 서버에서 첫 문장만 잘라 중단시키는 방식 검토

### 🟡 R4. 배경 제거 성능 — **절반만 해소 (2026-08-04)**

- ✅ **뒤쪽(변환 결과 누끼)은 해소됐다.** 코드 누끼 44ms로 대체. rembg 12~25초 절약.
- 🔴 **앞쪽(원본 사전 누끼)은 남아 있다.** 생략하면 캐릭터 일관성이 무너져서 뺄 수 없다.
  여전히 12~25초가 걸리고, ML Kit 온디바이스(~200ms) 이관 검토도 계속 유효하다.
  - ⚠️ ML Kit로 옮길 때는 **속도만이 아니라 실루엣 안정성**을 봐야 한다.
    경계가 지저분하면 그게 그대로 캐릭터 일관성 저하로 이어진다.

새로 생긴 위험은 R8(흰색 인형).

### 🟡 R8. 흰색·크림색 인형에서 코드 누끼 실패 가능

순백 배경 + flood fill 방식은 **캐릭터가 흰색이면 몸통이 배경으로 판정되어 잘려나간다.**
백곰·양·토끼 인형은 아이 애착인형에서 흔한 색이라 무시할 수 없다.

- 검증법: 흰색 봉제인형으로 파이프라인 실행
- 대안: 배경색을 마젠타(#FF00FF) 등 보색으로 지정 후 같은 flood fill (미검증)

---

## 작업 로그

날짜는 절대 날짜로 기록한다.

| 날짜 | 내용 |
|---|---|
| 2026-08-04 | 프로젝트 시작. Figma 기획 분석, CLAUDE.md 작성 |
| 2026-08-04 | 과제 1 착수 — 이미지 변환 후보 조사 완료, 비교 테스트 스크립트 작성 |
| 2026-08-04 | AI 파트 개발 환경 구축 (uv + Python 3.12 `.venv`, google-genai / rembg / pillow). 이미지 변환은 **Gemini 루트로 확정** |
| 2026-08-04 | 1차 실행 — 테스트 대상은 **초록/노랑 바다거북 인형**(`doll.jpg`). ✅ 배경 제거 성공(품질 우수) / ❌ 스타일 변환은 무료 티어 할당량 0으로 429 실패 |
| 2026-08-04 | $10 충전 완료. 프롬프트 v1→v2→v3 반복. **v2는 정체성 상실로 실패** (별 눈·X자 등껍질·손 흔드는 팔 = 완전히 다른 캐릭터). v3에서 회복 |
| 2026-08-04 | **Flash vs Pro 비교 완료 → `gemini-3.1-flash-image` 확정.** 누적 비용 약 390원 |
| 2026-08-04 | 캐릭터 애니메이션 방식 확정 — **사전 생성 스프라이트 + 실시간 코드 변형**. 영상 생성 방식은 배제 |
| 2026-08-04 | **과제 1 종료.** 미검증 리스크 R1~R4는 위 섹션에 기록. 다음 세션에서 과제 2(TTS) 시작 |
| 2026-08-04 | **과제 2 착수.** Gemini TTS 3개 모델 × voice 8종 실측 완료. 2.5 pro 탈락, 스트리밍 필수 확인(TTFB 1.3초). 립싱크 RMS 방식 검증 완료. ElevenLabs 키 발급 대기 중 |
| 2026-08-04 | 계정에서 **`gemini-3.1-flash-live-preview`(음성↔음성 Live API)** 확인 → 과제 4 후보로 기록 |
| 2026-08-04 | ElevenLabs 키 발급·실측. **Flash v2.5 TTFB 0.25~0.52초로 Gemini(1.3초) 대비 3~5배 우위** |
| 2026-08-04 | STT 역검증으로 한국어 명료도 확인(양쪽 94.7~100%). **LLM 청취 평가는 순위 재현 실패 → 폐기** |
| 2026-08-04 | 오디오 입력 지원 모델 실측 — TTS 모델은 `Audio input modality is not enabled`로 거부. `gemini-3.5-flash` / `gemini-3.1-flash-image` 는 오디오를 프롬프트로 받음(**음성만으로 이미지 생성 성공**) |
| 2026-08-04 | **과제 3 완료.** TTS 단독 대사 생성은 영어 지시일 때만 되고 한국어 출력이 무너짐(`킨더가튼`) → **LLM과 TTS를 분리 유지** |
| 2026-08-04 | **과제 4 완료.** 경로 A/B/C 실측 → **Live API 0.85초로 채택**. `thinking_budget=0`이 최대 레버(7.8초→2.1초)였음 |
| 2026-08-04 | **과제 2 완료.** Live 채택에 따라 목소리는 **Gemini voice로 확정**, ElevenLabs 미채택(부품은 빠르나 완성품이 3배 느림) |
| 2026-08-04 | **파이프라인 개정 — rembg 2회 → 1회.** Gemini는 알파를 못 만들지만(3/3 RGB) 순백 배경은 4/4 재현(254.7±0.6) → 변환 결과 누끼를 코드 44ms로 대체 |
| 2026-08-04 | ⚠️ **사전 누끼 생략은 반증됨.** 배경은 깨끗해지나 캐릭터가 매번 달라짐(자세·표정·무늬). 사전 누끼는 **일관성 장치**로 유지. R1과 직결 |

### 산출물 (과제 1)

| 파일 | 내용 |
|---|---|
| `ai/doll_stylize_test.py` | 파이프라인 + 프롬프트 v1/v2/v3 보관 |
| `ai/cutout.py` | **코드 누끼 (rembg 대체, 44ms).** `cutout.py in.png out.png` / `--demo` |
| `ai/out/01_cutout*.png` | 배경 제거 결과 (손까지 제거됨) |
| `ai/out/02_flash.png` | v1 — 정체성 최고, 무표정 |
| `ai/out/02_flash_v2.png` | v2 — ❌ 실패 사례. 정체성 상실 |
| `ai/out/02_flash_v3.png` | v3 — **현재 최선**. 표정 O, 뒷지느러미 X |
| `ai/out/02_pro_v3.png` | Pro 비교용 — 색 밝아짐, 코 추가됨 |
| `ai/out/compare_*.png` | 비교표 2장 |

### 산출물 (과제 3·4)

| 파일 | 내용 |
|---|---|
| `ai/dialog_test.py` | 왕복 검증 스크립트 (`--task3` / `--paths` / `--live` / `-n` / `--rep`) |
| `ai/out/dialog/child_*.wav` | 아이 발화 입력 3종 (TTS 합성) |
| `ai/out/dialog/*_a_*.wav` `*_b_*.wav` `*_c_live.wav` | 경로별 인형 응답 오디오 |
| `ai/out/dialog/task3_tts_*.wav` | TTS 단독 생성 시도 (영어/한국어 지시) |
| `ai/out/dialog/report_paths.json` | 경로별 TTFB·응답 기록 |

`DOLL_PERSONA` (인형 페르소나 프롬프트)가 `dialog_test.py` 에 있다. **앱에 하드코딩되어 출시되는 산출물**이므로
프롬프트 v1/v2/v3 와 같은 급으로 취급한다.

### 산출물 (과제 2)

| 파일 | 내용 |
|---|---|
| `ai/tts_test.py` | TTS 검증 스크립트 (`--voices` / `--styles` / `--engines` / `--eleven` / `--judge` / `--lipsync`) |
| `ai/out/tts/style_*.wav` | 같은 voice·다른 연기 지시 6종 (커스터마이징 범위 확인용) |
| `ai/out/tts/voice_*.wav` | Gemini voice 8종 샘플 (같은 대사) |
| `ai/out/tts/eleven_voice_*.wav` | ElevenLabs voice 5종 (인형 적합도 상위) |
| `ai/out/tts/eleven_model_*.wav` | ElevenLabs 모델 3종 비교 |
| `ai/out/tts/eng_*.wav` | Gemini 모델 3종 × 대사 3종 + 스트리밍 |
| `ai/out/tts/report_*.json` | 지연·토큰 사용량·평가 기록 |

---

## 프롬프트 튜닝에서 배운 것 (중요)

**모델 선택보다 프롬프트가 훨씬 큰 레버다.** 프롬프트는 앱에 하드코딩되어 출시되는 산출물이므로 과제 1의 핵심 결과물로 취급한다.

- **명시하지 않은 것은 지켜지지 않는다.**
  - "입 그려라" 안 씀 → 무표정으로 나옴 (v1)
  - "케어 라벨 지워라" 안 씀 → "MADE IN CHINA" 태그를 그릴 뻔함
  - "지느러미 4개 유지해라" 안 씀 → 뒷지느러미 2개를 생략함 (v3)
- **변형을 세게 요구하면 모델이 원본 참조를 포기하고 새로 그린다.**
  - v2에서 `chibi 비율` + `뒷다리로 서 있게`를 넣자 색만 남기고 전부 재설계됨
  - → **금지 지시(`Do NOT redesign`, `Do NOT turn into a generic mascot`)를 명시해야 막힌다**
- **귀여움 ↔ 정체성은 트레이드오프.** 이 서비스는 정체성이 생명이므로 귀여움 쪽으로 과하게 밀지 말 것.
- 거북이 하나로만 튜닝하면 과적합 위험 → **다른 인형 2~3종(곰인형 등)으로 교차 검증 필요** (미완료)

프롬프트 버전은 `ai/doll_stylize_test.py` 에 `STYLE_PROMPT`(v1), `PROMPT_V2`, `PROMPT_V3` 로 보관 중.

---

## 개발 환경 (AI 파트)

```
Desktop/hack/
├── CLAUDE.md
├── .env             # API 키 (git 제외)
├── .env.example     # 키 템플릿
├── .gitignore
├── .venv/           # Python 3.12 (uv 생성, git 제외)
└── ai/
    ├── doll_stylize_test.py   # 과제 1 검증 스크립트
    └── out/                   # 테스트 산출물 (git 제외)
```

실행: `.venv/bin/python ai/doll_stylize_test.py <사진경로>`

- 패키지 관리는 **uv** 사용 (`uv pip install --python .venv/bin/python <pkg>`)
- 시스템 Python(3.9)은 rembg 미지원이므로 **반드시 `.venv` 사용**
- API 키는 `.env`에만 두고 **절대 커밋하지 않는다**
- 해커톤이므로 **완성도보다 동작하는 데모**를 우선한다. 세팅에 오래 걸리는 선택지(자체 모델 학습 등)는 배제한다.
